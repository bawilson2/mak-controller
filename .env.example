# ==============================================================================
# General Container Configuration
# ==============================================================================
TIMEZONE=America/Los_Angeles
LOG_LEVEL=INFO
DATA_DIR=./data

# ==============================================================================
# Standard Bridge Mode (docker-compose.yml)
# Port to expose on the host. Must be reachable on port 80 for the Pellet Boss.
# ==============================================================================
HOST_PORT=80

# ==============================================================================
# Macvlan Mode (docker-compose.macvlan.yml)
# Used if host port 80 is occupied (e.g., TrueNAS SCALE, Synology, Unraid WebGUI)
# ==============================================================================
CONTAINER_IP=192.168.0.95
PARENT_INTERFACE=eno1
SUBNET=192.168.0.0/24
GATEWAY=192.168.0.1

# ==============================================================================
# Notifications (Optional - Can also be configured directly in the Web UI)
# ntfy topic name (e.g., my-mak-grill) or a self-hosted URL
# ==============================================================================
NTFY_TOPIC=
